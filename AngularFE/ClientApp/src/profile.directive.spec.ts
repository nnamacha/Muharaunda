import { ProfileDirective } from './profile.directive';

describe('ProfileDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfileDirective();
    expect(directive).toBeTruthy();
  });
});
